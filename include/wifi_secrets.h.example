/**
 * wifi_secrets.h.example — template for the gitignored Wi-Fi credentials
 * header. Copy this file to `include/wifi_secrets.h` (which is gitignored on
 * purpose) and fill in your own SSID and password before building.
 *
 *   cp include/wifi_secrets.h.example include/wifi_secrets.h
 */
#ifndef HYDROPONIC_MONITOR_WIFI_SECRETS_H
#define HYDROPONIC_MONITOR_WIFI_SECRETS_H

#define WIFI_SSID "your-ssid-here"
#define WIFI_PASSWORD "your-password-here"

#endif /* HYDROPONIC_MONITOR_WIFI_SECRETS_H */
