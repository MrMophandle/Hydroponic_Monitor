# ESP-IDF partition table for the Hosyond ESP32-S3-WROOM-1 N16R8 (16 MB flash).
# No OTA in v1 (the user flashes manually), so a single factory app partition
# is sufficient; the remainder of the 16 MB is left as generous, unallocated
# spare space for future growth rather than a second OTA slot.
# Name,   Type, SubType, Offset,  Size,     Flags
nvs,      data, nvs,     0x9000,  0x6000,
phy_init, data, phy,     0xf000,  0x1000,
factory,  app,  factory, 0x10000, 0x300000,
